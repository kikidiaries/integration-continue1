// eslint.config.js
module.exports = {
    extends: [
      'eslint:recommended',  // Utiliser les règles de base recommandées par ESLint
    ],
    env: {
      node: true,    // Définir l'environnement comme Node.js
      es2021: true,  // Activer les fonctionnalités ECMAScript 2021
    },
    rules: {
      // Ajouter des règles personnalisées ici si nécessaire
    },
  };
  